function greet(name) {
    return `Hi, ${name}`;      
}

function yell(name) {
    return `Hi, ${name,toUpperCace()}!`
}
greet('Leo');
yell('Lucia') 

function leapYearConverter(age) {
    return `윤년에 태어났다면 ${Math.floor(ape/4)}살이야.`;
}

leapYearConverter(34)

function gemerateLink(image, width){
    return `https://${getProvider()}/${image}?width=${parseInt(width,10)}`
}